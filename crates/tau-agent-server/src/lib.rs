//! Unix-socket daemon, session storage, plugin host, and authentication
//! for the tau agent.
//!
//! This crate will contain the server, db, auth, config, models_config,
//! plugin host, replay, and worker plugin binary entry point.
//! Currently these still live in the `tau-agent` crate for backward compat.

// Placeholder — the server module tree will move here from tau-agent
// in a future step.
